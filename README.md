# FreeformProgramming
In the git repository, there is 4 csv files, which, the powerpoint presentation, one python script which has everything needed to run the csv file datasets

Python Packages used: 
pandas, matplotlib, seaborn, numpy
imports for codes written below: 
##############
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import numpy as np
from numpy.polynomial.polynomial import polyfit
from matplotlib.ticker import MaxNLocator
from matplotlib.patches import Patch
##############
